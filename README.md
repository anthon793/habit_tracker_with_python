# habit_tracker_with_python
# Hit Tracker App

This is a simple hit tracker app that tracks hits (such as coding time, screen time for apps, etc.) and visualizes the data.

## Installation

```bash
pip install -r requirements.txt